# Design QA — CRM 工作台改版

- 桌面看板：`C:\Users\Yuan\Desktop\My\Coolapk-API-Collect\monitor-web\qa-crm-final.png`
- 详情弹窗：`C:\Users\Yuan\Desktop\My\Coolapk-API-Collect\monitor-web\qa-crm-detail.png`
- 手机布局：`C:\Users\Yuan\Desktop\My\Coolapk-API-Collect\monitor-web\qa-crm-mobile.png`
- 验收尺寸：1920 × 1080、1440 × 1024、390 × 844
- 实时状态：1 个监控话题、10 条动态、图片代理正常、详情与评论接口正常

## 本轮设计结果

- 信息架构调整为典型 CRM：深色业务侧栏、顶部全局操作区、指标卡、筛选工具栏和结构化动态数据表。
- 移除常驻详情栏与三栏拖拽布局；点击任意动态后，以居中模态弹窗展示完整正文、全部配图、互动数据、AI 判断和评论。
- 详情弹窗在桌面端采用“正文 / 评论”双栏，在手机端自动转为单列全屏阅读。
- 保留并统一监控添加、关键词搜索、AI 规则、飞书配置、AI 命中中心、图片放大与错误占位。
- 增加“全部话题”汇总视图、当前动态关键词筛选、AI 命中过滤与发布时间正倒序切换。
- 提升 1440px 与 1920px 字号、行高、头像和缩略图尺寸；大屏不再出现内容稀疏或阅读字号偏小的问题。

## 交互验收

- CRM 工作台加载 10 条实时动态，4 个关键指标和话题侧栏数据正确。
- 点击数据行后原页弹出详情，地址使用 `#feed-ID` 标记；关闭后列表状态和地址恢复。
- 已验证一条包含 14 条评论的动态，正文与评论均在弹窗内展示。
- 监控搜索弹窗可搜索“薅羊毛”，返回公开话题结果；添加入口与侧栏入口均响应。
- “全部话题”汇总视图、动态文本筛选、图片灯箱和设置入口均响应。
- 1920px、1440px、390px 均无页面级横向溢出；手机端详情为全屏单列。
- 浏览器控制台未发现 error 或 warning。

## 自动检查

- `node --check public/app.js`：通过
- `npm test`：3/3 通过
- `/api/health`：通过

## 结论

P0 / P1 / P2 视觉与交互问题均已关闭，CRM 改版验收通过。

## 2026-07-20 独立页面改版

- “添加监控”“AI 命中记录”“系统设置”已从模态弹窗迁移到 CRM 主内容区独立页面；侧栏、顶部入口、返回工作台和激活态均已联动。
- 动态详情继续使用弹窗，保持正文与评论的聚焦阅读体验。
- 添加监控改为“先搜索、后选择真实结果”，移除直接提交任意文本的入口；无结果时只展示换关键词提示。
- 服务端新增精确话题校验，错误请求返回 404 和候选结果，不再形成通用 500 错误。
- 实际使用“数码”完成搜索、添加、进入工作台和清理回归，完整链路通过。
- 桌面与手机端三类独立页面均无横向溢出，页面切换期间 `dialog[open]` 数量为 0。
- 验收图：`qa-crm-add-page.png`、`qa-crm-settings-page.png`、`qa-crm-ai-page.png`。
- 浏览器控制台 error / warning：0。

## 2026-07-20 搜索选中链路与整体协调性优化

- 修复“已从搜索结果选择，仍提示不是精确话题名”的问题：搜索结果会携带酷安返回的规范 `tag`，添加监控时不再用展示标题代替真实话题标识。
- 话题详情校验与动态列表抓取已解耦；详情有效但列表接口短暂波动时，仍可先建立监控并在下一轮定时任务补齐动态。
- 服务端增加 10 分钟搜索结果校验缓存，选中真实搜索结果后即使上游瞬时波动，也能根据已验证详情完成添加。
- 已分别用“优惠”和“数码”执行搜索 → 选择结果 → 添加监控 → 返回工作台 → 清理临时话题的端到端回归，两条链路均通过。
- 添加监控页增加常用搜索词、结果数量、引导与空状态；AI 命中记录增加汇总指标；系统设置统一页头、卡片和表单层级。
- 统一独立页面的圆角、阴影、留白、分隔线和进入动效；1440 × 1024 与 390 × 844 均无横向溢出。
- 当前运行状态：1 个监控话题（薅羊毛小分队）、10 条动态、0 个打开的弹窗，浏览器控制台 error / warning 为 0。
- 最终验收图：`qa-crm-ui-dashboard.png`、`qa-crm-ui-add.png`、`qa-crm-ui-search-result.png`、`qa-crm-ui-ai.png`、`qa-crm-ui-settings.png`。

## 2026-07-20 监控源类型修复

- 定位到酷安 `/topic/newTagDetail` 在 `iphone17` 等关键词下会返回 `entityType: product`；旧逻辑保存展示标题后，下一轮仍调用话题动态接口，因而出现“话题不存在”。
- 监控源模型新增 `sourceType`、`sourceId`、`sourceKey`，自动区分普通话题与数码产品。
- 普通话题继续使用 `/topic/tagFeedList`；数码产品改用 `/page/dataList?url=/product/feedList`，并按发布时间归一化为最新 10 条。
- 启动时会迁移旧数据。现有 `iPhone 17` 已识别为 `product:4283`，自动恢复 10 条动态，`lastError` 已清空。
- 搜索结果增加“话题 / 数码产品”类型标识，提交稳定的监控源键，别名与展示标题不再影响后续刷新。
- 使用 `iPhone 16` 完成搜索 → 添加 → 全量刷新 → 删除的真实回归；添加和刷新均返回 10 条数据、`lastError: null`。
- 页面端再次点击“刷新数据”成功，提示“全部话题已刷新”；`iPhone 17` 保持 10 条动态，错误提示未出现，浏览器控制台 error / warning 为 0。
- 验收图：`qa-source-refresh-fix.png`。

## 2026-07-20 AI 兼容接口连接修复

- 定位到当前配置为 `https://api.deepseek.com`，该服务使用 OpenAI 兼容的 `/chat/completions`；旧逻辑固定请求 `/responses`，测试连接返回 HTTP 404。
- 新增接口协议设置：自动识别、Responses API、Chat Completions；自动模式会优先匹配服务类型，并在端点不存在时切换到另一种兼容协议。
- 测试连接与正式帖子 AI 判断共用同一套协议适配，Responses API 保留 JSON Schema 输出，Chat Completions 使用 JSON Object 输出并支持图文消息。
- 实际配置 `deepseek-v4-flash` 已通过页面“测试 AI 连接”，显示 `Chat Completions 连接成功`；本地接口返回 HTTP 200。
- 浏览器控制台 error / warning 为 0，服务端新日志为空；验收图：`qa-ai-connection-success.png`。

## 2026-07-20 图片消息判断恢复

- `deepseek-v4-flash` 的 Chat Completions 实现仅接受文本内容，图片消息中的 `image_url` 会触发反序列化错误并形成 0% 判断异常。
- 新增图片能力自动降级：首次遇到该格式错误时，当前请求立即改用文本内容重试，并在运行期记住该模型不接收图片消息，后续抓取直接走文本判断。
- 已保留 Responses API 的图片输入；支持视觉输入的模型继续按图文联合判断。
- 已清理历史图片协议异常记录，成功判断会覆盖同一动态此前的图片格式异常。
- 对“薅羊毛小分队”执行真实 AI 分析回归：10 / 10 条完成、0 条图片协议异常；工作台当前 10 条动态均展示正常置信度和命中状态。
- 验收图：`qa-ai-judgement-recovered.png`。
